---
title: 蓝牙连接
hidden: true
abbrlink: b253b844
date: 2026-04-13 12:23:32
categories:
tags:
---
> 需要先学习《Android 中的权限》

### 声明蓝牙权限

要使应用能够连接蓝牙，首先要在 manifest.xml 文件中声明应用需要使用的权限。声明的蓝牙权限取决于应用的目标SDK版本（也就是安卓版本）。

如果目标平台为**安卓12**或更高，则应该获取以下权限：

1. 允许查找蓝牙设备

   ```XML
       <uses-permission android:name="android.permission.BLUETOOTH_SCAN"/>
   ```

2. 允许当前设备可被其他蓝牙设备检测

   ```XML
   <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE"/>
   ```

3. 允许与配对的蓝牙设备通信

   ```xml
   <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
   ```

4. 允许位置信息（可选）

   ```xml
   <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
   ```

如果目标平台为**安卓11**或更低版本，则应该获取下面的权限：

1. 允许蓝牙权限

   ```xml
   <uses-permission android:name="android.permission.BLUETOOTH"/>
   ```

2. 允许位置信息

   ```xml
   <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
   ```

但是现在的手机绝大部分都是安卓12及以上的，所以按需要（你的应用需不需要这些功能）直接声明第一类的权限即可

### 请求蓝牙权限

